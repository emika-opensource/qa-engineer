{
  "name": "qa-engineer",
  "version": "1.0.0",
  "description": "QA Engineer Dashboard — Emika AI Employee workspace for test management and automation",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "npx playwright test",
    "test:api": "npx playwright test tests/api/",
    "test:ui": "npx playwright test tests/ui/",
    "test:unit": "node --test tests/unit/"
  },
  "dependencies": {
    "express": "^4.18.0",
    "cors": "^2.8.0",
    "fs-extra": "^11.0.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.0"
  }
}
